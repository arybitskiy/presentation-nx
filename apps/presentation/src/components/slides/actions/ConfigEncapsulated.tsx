import { SlideType } from '../../../types';
import { Slide } from '../../Slide';

export const ConfigEncapsulated = () => (
  <Slide
    type={SlideType.ACTION}
    name="Config Encapsulated"
    time={1}
    dx={1}
    co={1}
    resolution={1}
  >
    <h1>
      Config Encapsulated, only people who know how to work with it - will do
      changes.
    </h1>
  </Slide>
);
