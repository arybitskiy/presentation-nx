import { SlideType } from '../../../types';
import { Slide } from '../../Slide';

export const SingleRepositroyForConfiguration = () => (
  <Slide
    type={SlideType.ACTION}
    name="Single Repository for Configuration"
    time={1}
    dx={1}
    co={1}
    resolution={1}
  >
    <h1>Single Repository for Configuration</h1>
  </Slide>
);
